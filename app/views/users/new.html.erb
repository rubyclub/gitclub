<h1>Join GitClub</h1>
<h3>The best way to design, build, and ship software.</h3>
<p> Step 1:      Step 2:       Step 3:</p>
<h2>Create your personal account</h2>
<div class="input-form-inner">
  <%= form_tag controller: "users", action: "new" do %>
    <% if flash[:alert].present? %>
      <p class="alert alert-danger"><%= flash[:alert] %></p>
    <% end %>

    <div class="table-row">
      <div class="table-cell name"><%= label_tag 'Username' %></div>
    </div>
    <div class="table-row">
      <div class="table-cell"><%= text_field_tag :name %></div>
    </div>

    <div class="table-row">
      <div class="table-cell name"><%= label_tag 'Email' %></div>
    </div>
    <div class="table-row">
      <div class="table-cell"><%= email_field_tag :email %></div>
    </div>

    <div class="table-row">
      <div class="table-cell name"><%= label_tag 'Password' %></div>
    </div>
    <div class="table-row">
      <div class="table-cell"><%= password_field_tag :password %></div>
    </div>

    <p>By clicking on "Create an account" below, your are agreeing
      to the Terms of Service and the Privacy Policy </p>

    <div class="table-row">
      <div class="table-cell"><%= submit_tag 'Create an account', class: 'btn btn-primary' %></div>
    </div>
  <% end %>
</div>